name: host_core
description: In-memory CommonCode host service with a desktop-safe Dart boundary.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  common_code_application:
    path: ../common_code_application
  common_code_domain:
    path: ../common_code_domain
  host_in_memory:
    path: ../host_in_memory

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
