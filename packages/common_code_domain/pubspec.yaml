name: common_code_domain
description: Placeholder CommonCode domain package for workspace wiring.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
