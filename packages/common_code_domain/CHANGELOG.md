## 1.0.0

- Replace the placeholder contract with the first shared domain model for
  `Session`, `PromptThread`, `Turn`, `Client`, and `Host`.
- Enforce the active host, active turn, and input client invariants in pure
  Dart.
