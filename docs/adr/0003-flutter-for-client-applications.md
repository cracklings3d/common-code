# Flutter for client applications

CommonCode will use Flutter for client applications so the same client stack can support desktop first and phone support later without splitting the UI implementation across frameworks. We considered Electron and other desktop-first GUI frameworks, but the phone requirement makes Flutter the better fit because it keeps desktop and mobile on one client stack.
